version: '3.8'

services:
  # API NestJS
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api
    restart: unless-stopped
    environment:
      PORT: 5000
      DB_HOST: mysql
      DB_PORT: 3306
      DB_USER: app_user
      DB_PASSWORD: app_password
      DB_NAME: app_db
    depends_on:
      mysql:
        condition: service_healthy
    labels:
      - 'traefik.enable=true'
      - 'traefik.http.routers.api.rule=Host(`mobile.maillet.bzh`)'
      - 'traefik.http.routers.api.entrypoints=websecure'
      - 'traefik.http.routers.api.tls=true'
      - 'traefik.http.routers.api.tls.certresolver=letsencrypt'
      - 'traefik.http.services.api.loadbalancer.server.port=5000'
      - 'traefik.docker.network=traefik'
    networks:
      - traefik
      - backend

  # Base de données MySQL
  mysql:
    image: mysql:8.0
    container_name: api_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: app_db
      MYSQL_USER: app_user
      MYSQL_PASSWORD: app_password
    command: ['--default-authentication-plugin=mysql_native_password']
    # Ne pas exposer le port MySQL publiquement sur l'hôte
    # (accessible uniquement depuis le réseau Docker)
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ['CMD', 'mysqladmin', 'ping', '-h', '127.0.0.1', '-proot']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - backend

  # Adminer (outil web pour MySQL) - optionnel
  adminer:
    image: adminer:latest
    container_name: adminer
    restart: unless-stopped
    environment:
      ADMINER_DEFAULT_SERVER: mysql
    depends_on:
      mysql:
        condition: service_healthy
    labels:
      - 'traefik.enable=true'
      - 'traefik.http.routers.adminer.rule=Host(`adminer.mobile.maillet.bzh`)'
      - 'traefik.http.routers.adminer.entrypoints=websecure'
      - 'traefik.http.routers.adminer.tls=true'
      - 'traefik.http.routers.adminer.tls.certresolver=letsencrypt'
      - 'traefik.http.services.adminer.loadbalancer.server.port=8080'
      - 'traefik.docker.network=traefik'
    networks:
      - traefik
      - backend

volumes:
  mysql_data:

networks:
  backend:
    driver: bridge
  traefik:
    external: true
    name: traefik
